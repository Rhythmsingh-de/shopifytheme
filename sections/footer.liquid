<footer class="site-footer site-footer--{{ section.settings.layout_style | default: 'columns' }}" role="contentinfo" {{ section.shopify_attributes }}>
  <div class="container">

    {%- assign has_brand_block = false -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'brand' -%}
        {%- assign has_brand_block = true -%}
      {%- endif -%}
    {%- endfor -%}

    <!-- Top row: brand + nav columns + newsletter -->
    {%- if section.settings.layout_style == 'modern-split' -%}
      <div class="footer-top footer-top--split">
        <div class="footer-split-left">
          {%- if has_brand_block == false and (section.settings.logo != blank or section.settings.tagline != blank or section.settings.social_instagram != blank or section.settings.social_facebook != blank or section.settings.social_tiktok != blank or section.settings.social_pinterest != blank or section.settings.social_youtube != blank) -%}
            <div class="footer-brand fallback-brand">
              {%- if section.settings.logo != blank -%}
                <a href="{{ routes.root_url }}" aria-label="{{ shop.name }} home">
                  <img
                    src="{{ section.settings.logo | image_url: width: 180 }}"
                    alt="{{ shop.name }}"
                    width="120" height="40"
                    loading="lazy" decoding="async">
                </a>
              {%- else -%}
                <a href="{{ routes.root_url }}" class="footer-brand__name" aria-label="{{ shop.name }} home">
                  {{ shop.name }}
                </a>
              {%- endif -%}

              {%- if section.settings.tagline != blank -%}
                <p class="footer-brand__tagline">{{ section.settings.tagline }}</p>
              {%- endif -%}

              <div class="footer-social" aria-label="Social media">
                {%- if section.settings.social_instagram != blank -%}
                  <a href="{{ section.settings.social_instagram }}" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"/><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/></svg>
                  </a>
                {%- endif -%}
                {%- if section.settings.social_facebook != blank -%}
                  <a href="{{ section.settings.social_facebook }}" target="_blank" rel="noopener noreferrer" aria-label="Facebook">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/></svg>
                  </a>
                {%- endif -%}
                {%- if section.settings.social_tiktok != blank -%}
                  <a href="{{ section.settings.social_tiktok }}" target="_blank" rel="noopener noreferrer" aria-label="TikTok">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.33 6.33 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34V8.69a8.18 8.18 0 0 0 4.78 1.52V6.76a4.85 4.85 0 0 1-1.01-.07z"/></svg>
                  </a>
                {%- endif -%}
                {%- if section.settings.social_pinterest != blank -%}
                  <a href="{{ section.settings.social_pinterest }}" target="_blank" rel="noopener noreferrer" aria-label="Pinterest">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M12 2a10 10 0 0 0-3.55 19.33c-.05-.72-.01-1.58.18-2.36l1.3-5.5s-.33-.66-.33-1.64c0-1.54.89-2.69 2-2.69.94 0 1.4.71 1.4 1.56 0 .95-.6 2.37-.92 3.69-.26 1.1.55 2 1.63 2 1.95 0 3.26-2.5 3.26-5.46 0-2.25-1.52-3.94-4.27-3.94-3.11 0-5.04 2.33-5.04 4.93 0 .9.27 1.53.68 2.02.19.22.22.31.15.57l-.25.97c-.08.32-.33.43-.6.31-1.68-.69-2.46-2.53-2.46-4.6 0-3.41 2.88-7.49 8.59-7.49 4.59 0 7.61 3.33 7.61 6.9 0 4.73-2.62 8.27-6.5 8.27-1.3 0-2.52-.7-2.94-1.49l-.82 3.17c-.25.94-.74 1.89-1.19 2.63.9.27 1.84.42 2.81.42A10 10 0 0 0 12 2z"/></svg>
                  </a>
                {%- endif -%}
                {%- if section.settings.social_youtube != blank -%}
                  <a href="{{ section.settings.social_youtube }}" target="_blank" rel="noopener noreferrer" aria-label="YouTube">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M22.54 6.42A2.78 2.78 0 0 0 20.58 4.44C18.88 4 12 4 12 4s-6.88 0-8.58.46A2.78 2.78 0 0 0 1.46 6.42 29 29 0 0 0 1 12a29 29 0 0 0 .46 5.58A2.78 2.78 0 0 0 3.42 19.56C5.12 20 12 20 12 20s6.88 0 8.58-.46a2.78 2.78 0 0 0 1.96-1.98A29 29 0 0 0 23 12a29 29 0 0 0-.46-5.58z"/><polygon points="9.75 15.02 15.5 12 9.75 8.98 9.75 15.02"/></svg>
                  </a>
                {%- endif -%}
              </div>
            </div>
          {%- endif -%}

          {%- for block in section.blocks -%}
            {%- if block.type == 'brand' or block.type == 'newsletter' -%}
              {%- render 'footer-block', block: block, section: section -%}
            {%- endif -%}
          {%- endfor -%}
        </div>
        <div class="footer-split-right">
          {%- for block in section.blocks -%}
            {%- if block.type == 'link_list' or block.type == 'text' -%}
              {%- render 'footer-block', block: block, section: section -%}
            {%- endif -%}
          {%- endfor -%}
        </div>
      </div>
    {%- else -%}
      <div class="footer-top">
        {%- if has_brand_block == false and (section.settings.logo != blank or section.settings.tagline != blank or section.settings.social_instagram != blank or section.settings.social_facebook != blank or section.settings.social_tiktok != blank or section.settings.social_pinterest != blank or section.settings.social_youtube != blank) -%}
          <div class="footer-brand fallback-brand">
            {%- if section.settings.logo != blank -%}
              <a href="{{ routes.root_url }}" aria-label="{{ shop.name }} home">
                <img
                  src="{{ section.settings.logo | image_url: width: 180 }}"
                  alt="{{ shop.name }}"
                  width="120" height="40"
                  loading="lazy" decoding="async">
              </a>
            {%- else -%}
              <a href="{{ routes.root_url }}" class="footer-brand__name" aria-label="{{ shop.name }} home">
                {{ shop.name }}
              </a>
            {%- endif -%}

            {%- if section.settings.tagline != blank -%}
              <p class="footer-brand__tagline">{{ section.settings.tagline }}</p>
            {%- endif -%}

            <div class="footer-social" aria-label="Social media">
              {%- if section.settings.social_instagram != blank -%}
                <a href="{{ section.settings.social_instagram }}" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"/><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/></svg>
                </a>
              {%- endif -%}
              {%- if section.settings.social_facebook != blank -%}
                <a href="{{ section.settings.social_facebook }}" target="_blank" rel="noopener noreferrer" aria-label="Facebook">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/></svg>
                </a>
              {%- endif -%}
              {%- if section.settings.social_tiktok != blank -%}
                <a href="{{ section.settings.social_tiktok }}" target="_blank" rel="noopener noreferrer" aria-label="TikTok">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.33 6.33 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34V8.69a8.18 8.18 0 0 0 4.78 1.52V6.76a4.85 4.85 0 0 1-1.01-.07z"/></svg>
                </a>
              {%- endif -%}
              {%- if section.settings.social_pinterest != blank -%}
                <a href="{{ section.settings.social_pinterest }}" target="_blank" rel="noopener noreferrer" aria-label="Pinterest">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M12 2a10 10 0 0 0-3.55 19.33c-.05-.72-.01-1.58.18-2.36l1.3-5.5s-.33-.66-.33-1.64c0-1.54.89-2.69 2-2.69.94 0 1.4.71 1.4 1.56 0 .95-.6 2.37-.92 3.69-.26 1.1.55 2 1.63 2 1.95 0 3.26-2.5 3.26-5.46 0-2.25-1.52-3.94-4.27-3.94-3.11 0-5.04 2.33-5.04 4.93 0 .9.27 1.53.68 2.02.19.22.22.31.15.57l-.25.97c-.08.32-.33.43-.6.31-1.68-.69-2.46-2.53-2.46-4.6 0-3.41 2.88-7.49 8.59-7.49 4.59 0 7.61 3.33 7.61 6.9 0 4.73-2.62 8.27-6.5 8.27-1.3 0-2.52-.7-2.94-1.49l-.82 3.17c-.25.94-.74 1.89-1.19 2.63.9.27 1.84.42 2.81.42A10 10 0 0 0 12 2z"/></svg>
                </a>
              {%- endif -%}
              {%- if section.settings.social_youtube != blank -%}
                <a href="{{ section.settings.social_youtube }}" target="_blank" rel="noopener noreferrer" aria-label="YouTube">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M22.54 6.42A2.78 2.78 0 0 0 20.58 4.44C18.88 4 12 4 12 4s-6.88 0-8.58.46A2.78 2.78 0 0 0 1.46 6.42 29 29 0 0 0 1 12a29 29 0 0 0 .46 5.58A2.78 2.78 0 0 0 3.42 19.56C5.12 20 12 20 12 20s6.88 0 8.58-.46a2.78 2.78 0 0 0 1.96-1.98A29 29 0 0 0 23 12a29 29 0 0 0-.46-5.58z"/><polygon points="9.75 15.02 15.5 12 9.75 8.98 9.75 15.02"/></svg>
                </a>
              {%- endif -%}
            </div>
          </div>
        {%- endif -%}

        {%- for block in section.blocks -%}
          {%- render 'footer-block', block: block, section: section -%}
        {%- endfor -%}
      </div>
    {%- endif -%}

    <!-- Trust bar -->
    {%- if section.settings.show_trust_bar -%}
      <div class="footer-trust" aria-label="Store guarantees">
        <div class="footer-trust-item">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/><path d="M3 3v5h5"/></svg>
          <div><strong>{{ settings.trust_returns_title | default: '30-Day Returns' }}</strong><span>{{ settings.trust_returns_sub | default: 'No questions asked' }}</span></div>
        </div>
        <div class="footer-trust-item">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><path d="M1 3h15v13H1zM16 8h4l3 3v5h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
          <div><strong>{{ settings.trust_shipping_title | default: 'International Shipping' }}</strong><span>{{ settings.trust_shipping_sub | default: '5–10 business days' }}</span></div>
        </div>
        <div class="footer-trust-item">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><polyline points="9 12 11 14 15 10"/></svg>
          <div><strong>{{ settings.trust_secure_title | default: 'Secure Checkout' }}</strong><span>{{ settings.trust_secure_sub | default: 'SSL encrypted' }}</span></div>
        </div>
        <div class="footer-trust-item">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
          <div><strong>{{ settings.trust_support_title | default: '24/7 Support' }}</strong><span>{{ settings.trust_support_sub | default: 'We reply within hours' }}</span></div>
        </div>
      </div>
    {%- endif -%}

    <!-- Bottom bar -->
    <div class="footer-bottom">
      <p class="footer-bottom__copy">&copy; {{ 'now' | date: '%Y' }} {{ shop.name }}. All rights reserved.</p>
      <nav class="footer-bottom__legal" aria-label="Legal links">
        {%- if shop.privacy_policy.url -%}
          <a href="{{ shop.privacy_policy.url }}">Privacy Policy</a>
        {%- endif -%}
        {%- if shop.terms_of_service.url -%}
          <a href="{{ shop.terms_of_service.url }}">Terms of Service</a>
        {%- endif -%}
        {%- if shop.refund_policy.url -%}
          <a href="{{ shop.refund_policy.url }}">Refund Policy</a>
        {%- endif -%}
        {%- if settings.show_order_tracking_link and settings.order_tracking_url != blank -%}
          <a href="{{ settings.order_tracking_url }}">{{ settings.order_tracking_label | default: 'Track Order' | escape }}</a>
        {%- endif -%}
      </nav>
      <div class="footer-bottom__payments" aria-label="Accepted payment methods">
        {%- for type in shop.enabled_payment_types -%}
          {{ type | payment_type_svg_tag: class: 'payment-icon' }}
        {%- endfor -%}
      </div>
    </div>

  </div>
</footer>

<style>
.site-footer {
  background: {{ section.settings.bg_color | default: 'var(--color-footer-bg, #111111)' }};
  color: {{ section.settings.text_color | default: 'var(--color-footer-text, #ffffff)' }};
  padding-block: var(--sp-16) var(--sp-8);
  margin-top: 0;
}
.site-footer a {
  color: {{ section.settings.text_color | default: 'var(--color-footer-text, #ffffff)' }};
  text-decoration: none;
  opacity: 0.75;
  transition: opacity var(--trans-base);
}
.site-footer a:hover { opacity: 1; }
.site-footer a:focus-visible {
  outline: 2px solid currentColor;
  outline-offset: 3px;
  border-radius: 2px;
}

.footer-top {
  display: grid;
  gap: var(--sp-10);
  padding-bottom: var(--sp-10);
  border-bottom: 1px solid rgba(255,255,255,0.1);
}

.footer-col {
  display: flex;
  flex-direction: column;
}

/* Styles implementation */

/* Footer Style 1 (Columns) */
.site-footer--columns .footer-top {
  grid-template-columns: 1.2fr repeat(auto-fit, minmax(200px, 1fr));
}
@media (max-width: 900px) {
  .site-footer--columns .footer-top {
    grid-template-columns: 1fr 1fr;
    row-gap: var(--sp-8);
  }
}
@media (max-width: 560px) {
  .site-footer--columns .footer-top {
    grid-template-columns: 1fr;
  }
}

/* Footer Style 2 (Centered) */
.site-footer--centered .footer-top {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: var(--sp-8);
}
.site-footer--centered .footer-col {
  align-items: center;
}
.site-footer--centered .footer-nav-col__list {
  align-items: center;
}
.site-footer--centered .footer-social {
  justify-content: center;
  margin-top: var(--sp-4);
}
.site-footer--centered .footer-brand__tagline {
  margin-inline: auto;
}
.site-footer--centered .footer-bottom {
  justify-content: center;
  text-align: center;
}

/* Footer Style 3 (Compact) */
.site-footer--compact {
  padding-block: var(--sp-8) var(--sp-5);
}
.site-footer--compact .footer-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: var(--sp-6);
  padding-bottom: var(--sp-5);
}
.site-footer--compact .footer-col {
  flex: 0 1 auto;
}
.site-footer--compact .footer-nav-col {
  display: none;
}
.site-footer--compact .footer-trust {
  padding-block: var(--sp-5);
}

/* Footer Style 4 (Minimal Grid) */
.site-footer--minimalist .footer-top {
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: var(--sp-8);
}
.site-footer--minimalist .footer-brand-col {
  order: 2;
}
.site-footer--minimalist .footer-nav-col {
  order: 1;
}
.site-footer--minimalist .footer-bottom {
  padding-top: var(--sp-8);
}
@media (max-width: 768px) {
  .site-footer--minimalist .footer-top {
    grid-template-columns: 1fr;
  }
}

/* Footer Style 5 (Modern Split Layout) */
.site-footer--modern-split .footer-top--split {
  display: grid;
  grid-template-columns: 1.2fr 2fr;
  gap: var(--sp-12);
  padding-bottom: var(--sp-10);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}
.site-footer--modern-split .footer-split-left {
  display: flex;
  flex-direction: column;
  gap: var(--sp-8);
}
.site-footer--modern-split .footer-split-right {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: var(--sp-8);
}
@media (max-width: 900px) {
  .site-footer--modern-split .footer-top--split {
    grid-template-columns: 1fr;
    gap: var(--sp-10);
  }
}

/* Footer Style 6 (Creative Clean Layout) */
.site-footer--creative-clean .footer-top {
  grid-template-columns: 1.5fr repeat(3, 1fr);
  gap: var(--sp-8);
}
.site-footer--creative-clean .footer-brand__name {
  font-size: 2.2rem;
  letter-spacing: -0.03em;
  font-weight: 800;
}
.site-footer--creative-clean .footer-brand__tagline {
  font-size: var(--text-sm);
  max-width: 32ch;
  line-height: 1.6;
}
@media (max-width: 1024px) {
  .site-footer--creative-clean .footer-top {
    grid-template-columns: 1.2fr repeat(2, 1fr);
  }
}
@media (max-width: 768px) {
  .site-footer--creative-clean .footer-top {
    grid-template-columns: 1fr;
  }
}

/* Shared Brand / Nav / Text Column styling */
.footer-brand__name {
  font-family: var(--font-display);
  font-size: var(--text-lg);
  font-weight: 700;
  opacity: 1 !important;
}
.footer-brand__tagline {
  font-size: var(--text-xs);
  opacity: 0.6;
  margin-top: var(--sp-2);
  line-height: 1.6;
  max-width: 26ch;
}
.footer-social {
  display: flex;
  gap: var(--sp-3);
  margin-top: var(--sp-4);
  flex-wrap: wrap;
}
.footer-social a {
  width: 38px; height: 38px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-full);
  border: 1px solid rgba(255,255,255,0.2);
  opacity: 0.7;
  transition: opacity var(--trans-base), border-color var(--trans-base);
}
.footer-social a:hover { opacity: 1; border-color: rgba(255,255,255,0.6); }

.footer-nav-col__title {
  font-size: var(--text-xs);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  opacity: 0.5;
  margin-bottom: var(--sp-4);
}
.footer-nav-col__list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: var(--sp-3);
}
.footer-nav-col__list a {
  font-size: var(--text-sm);
  opacity: 0.7;
}
.footer-nav-col__list a:hover { opacity: 1; }

/* Text block style */
.footer-text-col__content {
  font-size: var(--text-sm);
  line-height: 1.6;
  opacity: 0.75;
}

/* Newsletter styling */
.footer-newsletter-col {
  max-width: 100%;
}
.footer-newsletter-col__subheading {
  font-size: var(--text-sm);
  opacity: 0.7;
  margin-bottom: var(--sp-3);
  line-height: 1.5;
}
.footer-newsletter-form {
  margin-top: var(--sp-3);
  max-width: 360px;
}
.footer-newsletter-form__field-wrapper {
  display: flex;
  position: relative;
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: var(--radius-sm, 4px);
  overflow: hidden;
  background: rgba(255, 255, 255, 0.05);
  transition: border-color var(--trans-base);
}
.footer-newsletter-form__field-wrapper:focus-within {
  border-color: rgba(255, 255, 255, 0.4);
}
.footer-newsletter-form__input {
  flex: 1;
  background: transparent;
  border: none;
  color: inherit;
  padding: 12px 16px;
  font-family: inherit;
  font-size: var(--text-sm);
  min-height: 44px;
}
.footer-newsletter-form__input::placeholder {
  color: rgba(255, 255, 255, 0.4);
}
.footer-newsletter-form__input:focus {
  outline: none;
}
.footer-newsletter-form__button {
  background: #ffffff;
  color: #111111;
  border: none;
  padding: 12px 20px;
  font-size: var(--text-sm);
  font-weight: 600;
  cursor: pointer;
  min-height: 44px;
  transition: background 0.2s ease, opacity 0.2s ease;
}
.footer-newsletter-form__button:hover {
  background: rgba(255, 255, 255, 0.9);
}
.footer-newsletter-form__message {
  display: block;
  margin-top: var(--sp-2);
  font-size: var(--text-xs);
}
.footer-newsletter-form__message--error {
  color: #ff6b6b;
}
.footer-newsletter-form__message--success {
  color: #4cd137;
  font-weight: 500;
}

/* Trust bar */
.footer-trust {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--sp-6);
  padding-block: var(--sp-8);
  border-bottom: 1px solid rgba(255,255,255,0.1);
}
@media (max-width: 900px) {
  .footer-trust { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 560px) {
  .footer-trust { grid-template-columns: 1fr; }
}
.footer-trust-item {
  display: flex;
  align-items: flex-start;
  gap: var(--sp-3);
  opacity: 0.85;
}
.footer-trust-item svg { flex-shrink: 0; margin-top: 2px; }
.footer-trust-item strong { display: block; font-size: var(--text-xs); font-weight: 700; }
.footer-trust-item span   { display: block; font-size: var(--text-xs); opacity: 0.6; margin-top: 2px; }

/* Bottom bar */
.footer-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--sp-4);
  padding-top: var(--sp-6);
  flex-wrap: wrap;
}
.footer-bottom__copy {
  font-size: var(--text-xs);
  opacity: 0.5;
  max-width: none;
}
.footer-bottom__legal {
  display: flex;
  gap: var(--sp-4);
  flex-wrap: wrap;
}
.footer-bottom__legal a {
  font-size: var(--text-xs);
  opacity: 0.5;
}
.footer-bottom__legal a:hover { opacity: 0.85; }
.footer-bottom__payments {
  display: flex;
  gap: var(--sp-2);
  flex-wrap: wrap;
  align-items: center;
}
.footer-bottom__payments .payment-icon {
  height: 20px;
  width: auto;
  border-radius: 3px;
  opacity: 0.7;
}
@media (max-width: 640px) {
  .footer-bottom { flex-direction: column; align-items: flex-start; }
}
</style>

{% schema %}
{
  "name": "Footer",
  "tag": "footer",
  "class": "shopify-section-footer",
  "blocks": [
    {
      "type": "brand",
      "name": "Brand profile",
      "limit": 1,
      "settings": [
        { "type": "image_picker", "id": "logo", "label": "Logo image" },
        { "type": "range", "id": "logo_width", "min": 50, "max": 250, "step": 10, "default": 120, "unit": "px", "label": "Logo width" },
        { "type": "textarea", "id": "tagline", "label": "Tagline", "default": "Premium wellness products for you, your home & your pets." },
        { "type": "checkbox", "id": "show_social", "label": "Show social links", "default": true }
      ]
    },
    {
      "type": "link_list",
      "name": "Navigation menu",
      "limit": 5,
      "settings": [
        { "type": "text", "id": "heading", "label": "Heading", "default": "Quick Links" },
        { "type": "link_list", "id": "menu", "label": "Menu" }
      ]
    },
    {
      "type": "text",
      "name": "Rich text",
      "limit": 3,
      "settings": [
        { "type": "text", "id": "heading", "label": "Heading", "default": "About Us" },
        { "type": "richtext", "id": "content", "label": "Text content", "default": "<p>Share store details, promotions, or brand content with your customers.</p>" }
      ]
    },
    {
      "type": "newsletter",
      "name": "Newsletter signup",
      "limit": 1,
      "settings": [
        { "type": "text", "id": "heading", "label": "Heading", "default": "Subscribe" },
        { "type": "textarea", "id": "subheading", "label": "Subheading", "default": "Sign up for exclusive offers, updates, and more." },
        { "type": "text", "id": "button_label", "label": "Button label", "default": "Subscribe" }
      ]
    }
  ],
  "settings": [
    { "type": "image_picker", "id": "logo",    "label": "Fallback footer logo (if brand block is missing)" },
    { "type": "text",        "id": "tagline",  "label": "Fallback brand tagline",   "default": "Premium wellness products for you, your home & your pets." },
    { "type": "header", "content": "Design Settings" },
    { "type": "select", "id": "layout_style", "label": "Footer style", "options": [
      { "value": "columns", "label": "Footer Style 1 (Columns)" },
      { "value": "centered", "label": "Footer Style 2 (Centered)" },
      { "value": "compact", "label": "Footer Style 3 (Compact)" },
      { "value": "minimalist", "label": "Footer Style 4 (Minimal Grid)" },
      { "value": "modern-split", "label": "Footer Style 5 (Modern Split)" },
      { "value": "creative-clean", "label": "Footer Style 6 (Creative Clean)" }
    ], "default": "columns" },
    { "type": "color", "id": "bg_color",       "label": "Background color", "default": "#111111" },
    { "type": "color", "id": "text_color",     "label": "Text color",       "default": "#ffffff" },
    { "type": "header", "content": "Social links" },
    { "type": "url", "id": "social_instagram", "label": "Instagram URL" },
    { "type": "url", "id": "social_facebook",  "label": "Facebook URL" },
    { "type": "url", "id": "social_tiktok",    "label": "TikTok URL" },
    { "type": "url", "id": "social_pinterest", "label": "Pinterest URL" },
    { "type": "url", "id": "social_youtube",   "label": "YouTube URL" },
    { "type": "header", "content": "Trust bar" },
    { "type": "checkbox", "id": "show_trust_bar", "label": "Show trust bar above footer bottom", "default": true }
  ],
  "presets": [
    {
      "name": "Footer",
      "blocks": [
        {
          "type": "brand",
          "settings": {
            "tagline": "Premium wellness products for you, your home & your pets.",
            "show_social": true
          }
        },
        { "type": "link_list", "settings": { "heading": "Shop",    "menu": "main-menu" } },
        { "type": "link_list", "settings": { "heading": "Help",    "menu": "footer" } },
        {
          "type": "newsletter",
          "settings": {
            "heading": "Subscribe",
            "subheading": "Sign up for exclusive offers, updates, and more."
          }
        }
      ]
    }
  ]
}
{% endschema %}
