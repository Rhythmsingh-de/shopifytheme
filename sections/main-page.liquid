<section class="section">
  <div class="page-width" style="max-width:760px;">
    <h1 style="font-size:clamp(28px,4vw,42px);font-weight:900;letter-spacing:-.04em;margin:0 0 24px;">{{ page.title }}</h1>
    <div class="description">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
